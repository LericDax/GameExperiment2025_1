{
  "name": "three-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:skybox-manifest": "node ./scripts/generate-skybox-manifest.mjs",
    "predev": "npm run generate:skybox-manifest",
    "prebuild": "npm run generate:skybox-manifest",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.1.6"
  },
  "dependencies": {
    "three": "^0.180.0"
  }
}
